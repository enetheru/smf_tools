#ifndef UTIL_H
#define UTIL_H

#include <string>
#include <vector>
using namespace std;

void
valxval( string s, unsigned int &x, unsigned int &y );
vector<unsigned int> expandString( const char *s );
    
#endif //UTIL_H
